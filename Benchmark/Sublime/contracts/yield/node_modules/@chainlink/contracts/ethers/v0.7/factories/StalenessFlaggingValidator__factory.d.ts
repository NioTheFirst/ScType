import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { TransactionOverrides } from "..";
import { StalenessFlaggingValidator } from "../StalenessFlaggingValidator";
export declare class StalenessFlaggingValidator__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(flagsAddress: string, overrides?: TransactionOverrides): Promise<StalenessFlaggingValidator>;
    getDeployTransaction(flagsAddress: string, overrides?: TransactionOverrides): UnsignedTransaction;
    attach(address: string): StalenessFlaggingValidator;
    connect(signer: Signer): StalenessFlaggingValidator__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): StalenessFlaggingValidator;
}
//# sourceMappingURL=StalenessFlaggingValidator__factory.d.ts.map