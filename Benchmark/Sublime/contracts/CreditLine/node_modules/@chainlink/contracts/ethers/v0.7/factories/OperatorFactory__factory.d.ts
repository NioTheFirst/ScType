import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { TransactionOverrides } from "..";
import { OperatorFactory } from "../OperatorFactory";
export declare class OperatorFactory__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(linkAddress: string, overrides?: TransactionOverrides): Promise<OperatorFactory>;
    getDeployTransaction(linkAddress: string, overrides?: TransactionOverrides): UnsignedTransaction;
    attach(address: string): OperatorFactory;
    connect(signer: Signer): OperatorFactory__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): OperatorFactory;
}
//# sourceMappingURL=OperatorFactory__factory.d.ts.map