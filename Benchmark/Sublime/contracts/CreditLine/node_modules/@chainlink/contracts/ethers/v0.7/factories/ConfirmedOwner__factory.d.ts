import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { TransactionOverrides } from "..";
import { ConfirmedOwner } from "../ConfirmedOwner";
export declare class ConfirmedOwner__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(newOwner: string, overrides?: TransactionOverrides): Promise<ConfirmedOwner>;
    getDeployTransaction(newOwner: string, overrides?: TransactionOverrides): UnsignedTransaction;
    attach(address: string): ConfirmedOwner;
    connect(signer: Signer): ConfirmedOwner__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): ConfirmedOwner;
}
//# sourceMappingURL=ConfirmedOwner__factory.d.ts.map